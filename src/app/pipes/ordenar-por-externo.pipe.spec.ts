import { OrdenarPorExternoPipe } from './ordenar-por-externo.pipe';

describe('OrdenarPorExternoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorExternoPipe();
    expect(pipe).toBeTruthy();
  });
});
